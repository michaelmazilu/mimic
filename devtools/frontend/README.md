# Mimic Frontend (Next.js)

Glass-box debug dashboard for the Mimic backend.

## Run

```bash
npm install

# default backend: http://localhost:8000
npm run dev
```

## Config (env)

- `NEXT_PUBLIC_BACKEND_URL` (default: `http://localhost:8000`)

